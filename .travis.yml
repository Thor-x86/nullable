language: go

go:
  - "1.3"
  - "1.5"
  - master

dist: focal

services:
  - docker

before_script:
  - docker-compose up -d --build
  - sleep 1m # Making sure all databases ready

script:
  - ./test_all.sh

after_script:
  - docker-compose down